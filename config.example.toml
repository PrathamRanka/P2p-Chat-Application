# Example runtime configuration for iroh-gossip-cli
data_dir = "data"
log_level = "info"
relay_mode = "default" # options: default, disabled
alpns = ["iroh-gossip"]

# Optional: override the bind addresses (array of strings)
# bind_addrs = ["0.0.0.0:0", "[::]:0"]
